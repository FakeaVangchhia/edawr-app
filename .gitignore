node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects, when generated by `expo prebuild` / `expo run:*`. They are
# reproducible from app.json, and checking them in means every config change
# has to be made twice.
/ios
/android

*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

.env
.env.local
npm-debug.*
.DS_Store
